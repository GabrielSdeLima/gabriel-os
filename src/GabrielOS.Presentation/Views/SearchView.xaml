<UserControl x:Class="GabrielOS.Presentation.Views.SearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:GabrielOS.Presentation.Converters">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <StackPanel MaxWidth="720">
        <TextBlock Text="Search" Style="{StaticResource H1}" Margin="0,0,0,16"/>

        <Border Style="{StaticResource Card}">
            <StackPanel>
                <Grid Margin="0,0,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource DarkTextBox}"
                             Margin="0,0,8,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Return" Command="{Binding SearchCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    <Button Grid.Column="1" Content="Search" Style="{StaticResource PrimaryButton}"
                            Command="{Binding SearchCommand}" Margin="0,0,8,0"/>
                    <Button Grid.Column="2" Content="Clear" Style="{StaticResource SecondaryButton}"
                            Command="{Binding ClearSearchCommand}"/>
                </Grid>
            </StackPanel>
        </Border>

        <TextBlock Text="{Binding ResultSummary}" Style="{StaticResource SecondaryText}"
                   Margin="0,0,0,12"
                   Visibility="{Binding HasSearched, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <ItemsControl ItemsSource="{Binding Results}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource Card}">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding EntityType}" Style="{StaticResource SecondaryText}"
                                           Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold"/>
                                <TextBlock Text=" · " Style="{StaticResource SecondaryText}"/>
                                <TextBlock Text="{Binding Date, StringFormat='{}{0:dd MMM yyyy}'}"
                                           Style="{StaticResource SecondaryText}"/>
                            </StackPanel>
                            <TextBlock Text="{Binding Title}" Style="{StaticResource BodyText}"
                                       FontWeight="SemiBold" Margin="0,4,0,2"/>
                            <TextBlock Text="{Binding Snippet}" Style="{StaticResource SecondaryText}"
                                       TextWrapping="Wrap" MaxHeight="60" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
